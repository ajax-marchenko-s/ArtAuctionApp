name: Build

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: '*'

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run gradle build
        run: ./gradlew build
