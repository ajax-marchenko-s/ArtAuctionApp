name: Playground for Svitlana Marchenko workflow1
run-name: SMarchenko Reusable workflow workflow1

on:
  workflow_call:
    inputs:
      name:
        description: 'Your name'
        type: string
        required: true

jobs:
  first-job:
    uses: ./.github/workflows/my-workflow1.yaml
    with:
      name: ${{ inputs.name }}
